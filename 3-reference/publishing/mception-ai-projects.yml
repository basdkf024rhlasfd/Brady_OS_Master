# MOVED — canonical source is now portal/src/config/projects.yml
# This file is kept as a pointer only. Do not edit project entries here.
# All project metadata (slugs, labels, access, magic links) lives in the
# unified config that drives the portal app directly.
