# intake/roles/

Drop job postings (current or old), role descriptions, and "what good looks like" notes here.
A weak existing posting is useful — it's the starting clay for `jd-optimizer`.

**Feeds:** `jd-optimizer`, `interview-guide-builder`, `onboarding-ramp`, `workforce-plan`.
**Confidential — git-ignored.** Only this README ships.

Naming that helps: `<role>-current-posting.md`, `<role>-notes.md` (e.g. `rsa-current-posting.md`).
