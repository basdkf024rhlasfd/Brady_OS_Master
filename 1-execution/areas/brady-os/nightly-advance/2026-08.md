# Nightly Advance — 2026-08

Log for `nightly-advance` (`3-reference/skills/nightly-advance/SKILL.md`). One row per idea.

`Outcome` is `approved` / `ignored` / `retired`. Per-lever acceptance rate over the trailing
30 days drives the weekly recalibration; ideas ignored twice move to **Retired Ideas** below
and are never sent again.

| Date | Lever | Idea | Signal | Size | Outcome |
|---|---|---|---|---|---|
| 2026-08-11 | collect | Execute SPEC-012 — reconcile the build queue against reality | Queue dormant ~12 weeks; 8 spec bodies said `Status: open` while filenames said `needs-review`; SPEC-015's deliverable verifiably shipped and was still marked open | 30m | **approved** (Brady: "advance something") |
| 2026-08-11 | compound | — not sent | Yield already advanced twice today (`/compound` + `nightly-advance` both landed); no third capability gap with a real signal | — | — |
| 2026-08-11 | cut | — not sent | 3 orphan skills found (`brain-dump-triage`, `kids-test-results-onepager`, `roborock-control`) but all 3 are <90d old and plausibly pre-positioned, not dead. Retiring them on zero-reference alone would be a bad call this early | — | — |

**Run 1 self-score: 10/10.** Every idea traced to a named Phase 1 signal; levers distinct;
sizes honest; no duplicates (first run); log + ledger rows written. Sent one idea and named
the two empty levers rather than padding to three — per the skill, that is a full-score run.

**Note on this run:** invoked on demand ("Advance something"), not on the 8 PM schedule, and
Brady delegated the pick rather than replying `advance N`. Executed the top-ranked idea
directly. First scheduled run fires 2026-08-11 ~20:06 CT.

## Observations for the weekly recalibration

- **Loudest unactioned signal in the repo:** `approve musashi musashi-13` appears **36 times**
  across gitted backups — re-proposed nightly for weeks, never answered. Also `approve heidi
  repo-r7` (11×) and `approve musashi musashi-16` (9×). This is the single largest Collect
  reservoir and should be a candidate idea on an upcoming run. Not sent tonight because the
  build-queue reconciliation was better-scoped and independently verifiable.
- **Anti-nag check:** an agent re-proposing the same gate 36 times is exactly the failure mode
  `nightly-advance` retires-after-two-ignores is designed to prevent. Worth raising with
  Musashi's own review loop.

## Retired Ideas

_(none yet — an idea lands here after being ignored twice)_
