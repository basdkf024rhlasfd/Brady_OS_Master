.DS_Store
.env
3-reference/skills/*/references/family-data.md
3-reference/skills/*/references/school-calendar.md
3-reference/skills/financial-assistant/data/*.csv

# Live cockpit data (contains PII, financials, family schedules)
os-cockpit/data.js
# portal/public/financial-assistant/data.js — committed so Vercel serves it

# Portal (mception.ai Next.js app)
portal/node_modules/
portal/.next/
portal/.env.local
.vercel
.env*.local

# Workspace-local Claude settings — SessionStart hook stays scoped to Rome only.
# Do NOT commit. If you want another workspace (bilbao, san-jose, etc.) to prime,
# create its own .claude/settings.json there.
.claude/

# Next.js build traces (root-level stray build artifacts)
.next/

# Generated workshop artifacts (renders + Midjourney images) — sources (.html/.md) stay tracked
3-reference/skills/*/output/*.pdf
3-reference/skills/*/output/images*/
/output/*.pdf
/output/images*/
3-reference/public-pack.zip
